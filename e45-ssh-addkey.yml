---
- hosts: all
  sudo: yes
  gather_facts: no
  remote_user:  root

  tasks:

  - name: install ssh key
    authorized_key: user=root 
                    key= https://github.com/ycl2045/ansible-demo/10.0.106.36-key.pub
                    state=present
